import 'package:flutter/material.dart';

class CheeseColor {
  static Color? error = Colors.red[700];
  static Color? gray = Colors.grey[600];
  static Color? bgColor = Colors.grey[100];
  static const Color input = Color.fromARGB(255, 245, 245, 245);
}
